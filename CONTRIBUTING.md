# Contributing

Use the root `dev.cmd` or `dev.sh` workflow and keep the plugin compatible with
an unmodified stock SageTV server. Do not commit credentials, `Sage.jar`, live
`ffmpeg.real.ini`, generated packages, or FFmpeg binaries.
